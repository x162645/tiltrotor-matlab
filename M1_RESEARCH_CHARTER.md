# M1 物理增强低阶模型研究章程

## 1. 版本起点

- M0 冻结来源仓库：`x162645/tiltrotor-matlab`
- M0 冻结分支：`frozen/m0-xv15-hover-v1-20260828`
- M0 冻结提交：`27f40883633ca14acc0e928649b62d7abb855491`
- M1 当前临时研究分支：`research/m1-xv15-physics-enhanced-20260828`
- 未来独立仓库建议名：`x162645/tiltrotor-matlab-m1`

当前 GitHub 连接器没有创建新仓库的写入能力，因此本分支作为可整体迁移的 M1 启动工作区。M0 冻结分支不得再用于模型物理修改。

## 2. M0 的冻结结论

M0 只允许声明：在 XV-15 原始金属桨 OARF 悬停数据的已支持约 6°–11°总距区间内，M0 能得到稳定物理解，并能复现 CT、CP 随总距增加而增加的定性趋势；Run 14 与 Run 15 均出现相似的系统残差结构。

M0 不允许声明：

- 已完成 XV-15 定量复现；
- 已通过 XV-15 旋翼性能验模；
- 50%–65% 的 CT/CP 偏差可以忽略；
- 低总距不收敛区属于可信适用域。

## 3. M1 研究目标

M1 的目标不是把 OARF 曲线“调好看”，而是研究哪些有独立来源依据的物理增强能够提高低阶模型的定量预测能力和跨工况可信度。

禁止：

- 根据 OARF CT/CP/FM 反求推力增益、功率增益或经验倍率；
- 根据 OARF 误差反求固定总距偏置；
- 通过搜索参数使 Run 15 MAPE 最小；
- 删除高误差点或失败点；
- 把 M1 的结果反写为 M0；
- 在看到未来独立验证数据后重新定义 M1 阶段模型。

允许：

- 采用 NASA 公开 XV-15 几何、C81、质量属性和明确可追溯的结构/气动参数；
- 增加有文献依据的新模型机制，并赋予新的模型身份；
- 在 Run 15 上进行开发性外部相关性研究，但所有参数必须来自外部资料或事先声明的建模假设；
- 在每一级模型冻结后再进入下一层物理增强。

## 4. 第一阶段模型阶梯

### M1-A：XV-15 截面气动 + 真实径向几何

目标：同时保留 XV-15 原始金属桨公开径向弦长和非线性扭转，并使用独立来源 C81 标量约化，继续采用全局低阶动量闭合。

回答：M0 中“通用截面气动 + 常弦/线性扭转”的降阶一共解释多少误差。

### M1-B：完整径向/马赫数 C81

目标：在 M1-A 几何基础上使用公开四段 C81 表和局部马赫数，不再把截面气动压缩为单组标量。

回答：径向翼型差异和局部马赫数依赖是否进一步改善 CT/CP。

### M1-C：分环带动量闭合

目标：在 M1-B 基础上把单一全局诱导速度改为独立径向环带动量闭合。

回答：简单径向入流分布本身能解释多少剩余偏差。

注意：M1-C 仍不等于非局部尾迹模型；若残差仍大，下一阶段才研究规定螺旋尾迹/升力线/Biot-Savart。

## 5. 第一阶段判据

同一 MATLAB R2021a 工作流必须同时生成：

1. 纯 M0 Run 15 6°–11°基线；
2. M1-A 结果；
3. M1-B 结果；
4. M1-C 结果；
5. 每一级 CT/CP/FM MAPE；
6. 每一级相对 M0 的误差变化；
7. 收敛状态和失败点；
8. 模型身份与参数来源说明。

第一阶段只做归因，不设置“必须把误差降到某个目标值”的通过门槛。

## 6. 第二阶段预留

如果第一阶段仍存在显著系统残差，按以下优先级研究：

1. 旋转受载后的真实桨距/弹性扭转输入契约；
2. 规定螺旋尾迹、升力线或 Biot-Savart 非局部诱导；
3. 雷诺数、粗糙度、转捩和压缩性影响；
4. CP 比较中的旋转毂及其他旋转件扭矩边界。

未经来源闭合，不允许用 OARF 反推上述参数。

## 7. 独立验证策略

OARF Run 15 固定为 M1 开发性外部相关性数据；Run 14 已被分析者读取，不能作为盲验证。

优先将不同设施/试验系列的 XV-15 原始金属桨 WADC 数据保留为后续独立设施级外部验证候选。在 M1 第一阶段模型身份冻结前，不使用 WADC 输出结果选择模型或调整参数。

## 8. 论文目标

最终研究问题不是“怎样拟合 XV-15”，而是：

> 通用低阶倾转旋翼模型在何种物理简化下只能保持趋势预测；哪些有来源约束的物理增强能够显著改善定量预测；模型增强后能否在独立试验设施/工况中保持误差改善，从而形成可解释的可信适用域。
