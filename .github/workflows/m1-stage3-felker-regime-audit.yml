name: M1 stage-3 XV-15 Felker regime audit

on:
  push:
    branches:
      - research/m1-xv15-physics-enhanced-20260828
    paths:
      - '.github/workflows/m1-stage3-felker-regime-audit.yml'
      - 'analysis/run_m1_stage3_felker_regime_audit.m'
      - 'analysis/run_m1_stage3_inboard_high_alpha_audit.m'
      - 'analysis/xv15_c81_section_lookup.m'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  m1-stage3-felker-regime-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up MATLAB R2021a
        uses: matlab-actions/setup-matlab@v3
        with:
          release: R2021a

      - name: Audit corrected Felker regime
        uses: matlab-actions/run-command@v3
        with:
          command: >-
            startup;
            root = pwd;
            artifactRoot = fullfile(root,'ci_artifacts','m1_stage3_felker_regime_audit');
            if ~exist(artifactRoot,'dir'), mkdir(artifactRoot); end;
            results = run_m1_stage3_felker_regime_audit(artifactRoot);
            fid = fopen(fullfile(artifactRoot,'M1_STAGE3_FELKER_STATUS.txt'),'w');
            fprintf(fid,'status=EXECUTED\nclaimBoundary=%s\n',results.claimBoundary);
            fclose(fid);

      - name: Upload corrected stage-3 evidence
        uses: actions/upload-artifact@v4
        with:
          name: m1-stage3-felker-regime-audit-evidence
          path: ci_artifacts/m1_stage3_felker_regime_audit
          if-no-files-found: error
