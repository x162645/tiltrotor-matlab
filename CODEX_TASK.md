# CODEX_TASK.md

STATUS: M1 XV-15 PHYSICS-ENHANCED LOW-ORDER RESEARCH / STAGE 5 CROSS-FACILITY HOLDOUT VALIDATION / 2026-08-28

## 版本契约

- 仓库：`x162645/tiltrotor-matlab`。
- M0 冻结分支：`frozen/m0-xv15-hover-v1-20260828`。
- M0 冻结提交：`27f40883633ca14acc0e928649b62d7abb855491`。
- M1 研究分支：`research/m1-xv15-physics-enhanced-20260828`。
- M1 Draft PR：#71。
- 未经用户明确授权不得合并 PR #71。

## 项目主线

本项目研究通用低阶倾转旋翼模型的分层外部验证、模型形式诊断和可信适用域，不以复现 XV-15 数字孪生为目标。

M0 保持冻结。M1 只允许采用独立来源约束的物理增强。OARF Run 14/15 已被用于开发性相关/诊断，不能继续用于反调参数并重新称为验证。

## 已完成证据链

同等级 MATLAB R2021a 结果：

- M0：CT MAPE 56.4224%，CP 62.6130%，FM 23.0180%；
- 截面气动桥接：42.8716%，51.1184%，12.2221%；
- M1-A 真实径向几何 + 标量 C81：33.9549%，45.2392%，4.4100%；
- M1-B 真实径向几何 + 完整径向 C81：37.8538%，50.5150%，7.5480%；
- M1-C M1-B + 分环带动量：35.7519%，47.9006%，5.9849%；
- M1-D 来源约束受载扭转：受载方向减小桨距，不能支持未知正桨距偏置作为主要残差解释；
- M1-E-1 通用 Corrigan n=1：32.7287%，45.8916%，7.5923%；
- M1-E-2 Koning XV-15/OARF 相关 n=1.8：28.3075%，41.7337%，8.0567%，但属于非独立相关性复现，不能作为 holdout 主模型。

## Stage 4 M1-F 非局部尾迹结论

M1-F 使用 Landgrebe 规定尾迹 + 多条离散尾涡 + 有限段 Biot-Savart，并以动量理论固定盘面平均诱导速度。

正式 MATLAB 运行表明：

- M1-F0 均匀动量控制与 M1-E-1 基本一致，说明移除旧方位入流形状本身几乎无影响；
- M1-F1 在 7°–11°存在数值稳定的非局部尾迹固定点，CT/CP 误差相对 M1-E-1 改善约数个百分点；
- 6°虽然可在数值审计中达到固定点残差标准，但出现负功率及极端正/负径向诱导速度，属于病态/非物理解；
- 当前内段尾迹采用经典 Landgrebe 归一化收缩的假设推广，且高度扭转 XV-15 桨叶超出经典 Landgrebe 数据库的理想适用范围。

因此 M1-F 保留为 `MODEL_FORM_DIAGNOSTIC`，不晋升为固定 6°–11°主模型。不得根据 Run 15 继续调尾迹参数。

## M1 holdout 冻结

在读取/执行 WADC 数据之前，已建立 `docs/M1_HOLDOUT_FREEZE.md`。

冻结主模型：

`M1_HOLDOUT_V1 = M1_E1_GENERIC_CORRIGAN_N1`

固定物理内容：

1. XV-15 原始金属桨真实径向弦长；
2. XV-15 原始金属桨非线性扭转；
3. 四径向区域完整 C81 + 局部马赫数；
4. 通用 Corrigan–Schillings `n=1`；
5. 全盘均匀动量诱导闭合；
6. 当前低阶一阶谐波挥舞；
7. 无 CT/CP 增益、无固定总距偏置、无 OARF 拟合。

冻结提交起点：`d313296a35319dc8a5e6c398adbed0d54e0f8ede`。

## Stage 5：WADC 跨设施外部验证

目标只回答：冻结的 `M1_HOLDOUT_V1` 相对冻结 M0，在不同设施/试验系列、不同转速或马赫范围中是否仍保持可解释的预测改善，以及其可信适用域是否扩大。

执行顺序：

1. 先审计 WADC 原始金属桨数据来源、设施、构型、总距定义、转速/尖速、系数定义与不确定度；
2. 建立 WADC 数据角色台账，明确它未被用于 `M1_HOLDOUT_V1` 参数选择；
3. 预声明可比较窗口和排除规则，只允许由数据定义/模型物理支持性决定，不允许按误差选点；
4. 在同一 MATLAB workflow 中同时执行冻结 M0 与 `M1_HOLDOUT_V1`；
5. 输出逐点 CT/CP/FM、MAPE、有符号误差、支持/失败状态及相对 M0 改善；
6. 与 OARF Run 14/15 的残差结构做跨设施比较，但不得据此回改模型；
7. 根据结果界定 M0 与 M1 的可信适用域。

## Stage 5 停止规则

开始读取 WADC 数值后：

- 禁止修改 Corrigan 指数；
- 禁止修改 C81 或径向几何以降低 WADC 误差；
- 禁止 WADC 反求总距偏置；
- 禁止经验 CT/CP/FM 增益；
- 禁止按 WADC 误差选择诱导模型或尾迹模型；
- 禁止删除失败点或高误差点；
- 禁止事后改变预声明窗口；
- 禁止将 WADC 同时作为参数开发数据和独立验证证据；
- 若跨设施改善失败，直接记录并收缩可信域，不修改冻结模型。

## 后续分叉规则

只有在 WADC 外部验证完成后，才决定：

- 若 M1_HOLDOUT_V1 改善可泛化：形成 M1 可信域并进入动力学/其他层级验证；
- 若不能泛化：保留失败证据，分析误差源，但不得回改该冻结身份；
- 若确需更高阶自由尾迹/多尾迹合并模型：建立新的 M2 模型身份，并使用新的开发/验证分割，不能用 WADC 调好后继续称其为同一 holdout。
