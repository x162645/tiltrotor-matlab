# AGENTS.md

## 本机 MATLAB 环境

本机完整 MATLAB 版本预计为 MATLAB R2021a。

MATLAB 可执行文件固定路径：

```text
F:\matlab\R2021a\bin\matlab.exe
```

项目根目录：

```text
E:\tiltrotor
```

在 PowerShell 中运行 MATLAB 时，优先使用完整路径，不依赖系统 PATH：

```powershell
& 'F:\matlab\R2021a\bin\matlab.exe' -batch "cd('E:\tiltrotor'); run('startup.m');"
```

运行项目测试时，可在确认入口文件实际存在后使用：

```powershell
& 'F:\matlab\R2021a\bin\matlab.exe' -batch "cd('E:\tiltrotor'); run('startup.m'); summary = run_all_checks;"
```

运行总演示时使用：

```powershell
& 'F:\matlab\R2021a\bin\matlab.exe' -batch "cd('E:\tiltrotor'); run('startup.m'); run('run_demo.m');"
```

不得因为 `matlab` 命令不在 PATH 中就判断本机没有安装 MATLAB。必须先检查上述完整路径。

若上述路径不存在或执行失败，应报告真实错误，不得切换到 MATLAB Runtime 冒充完整 MATLAB。


## 1\. 项目身份

本项目是一个 MATLAB 倾转旋翼机部件级正向机理建模项目。

项目根目录：

```text
E:\\\\tiltrotor
```

Codex 应将该目录视为唯一工作区。除非用户明确授权，不得读取、修改、创建或删除该目录之外的文件。

本项目当前目标是建立一个：

* 可以运行；
* 可以计算；
* 坐标系和符号一致；
* 单位一致；
* 数值上可检查；
* 物理关系自洽；
* 可以逐步替换为 XV-15 公开数据并进行验证；

的倾转旋翼机模型。

当前阶段不要求严格复现 XV-15，也不得把尚未验证的模型描述成“XV-15 高保真模型”“已经完成 XV-15 复现”或“已经通过试验验证”。

\---

## 2\. 项目主要文件

Codex 必须先扫描项目目录，以实际文件为准。预计主要 MATLAB 文件包括：

```text
run\\\_demo.m
params\\\_nominal.m
trim\\\_symmetric.m
tiltrotor\\\_eom.m
total\\\_forces\\\_moments.m
mass\\\_properties.m
rotor\\\_model\\\_bemt.m
wing\\\_model.m
fuselage\\\_model.m
horizontal\\\_tail\\\_model.m
vertical\\\_tail\\\_model.m
linearize\\\_numeric.m
stability\\\_report.m
```

项目还可能包含其他函数、脚本、测试文件和数据文件。不得只检查上面列出的文件。

参考文献目录：

```text
E:\\\\tiltrotor\\\\references
```

当前参考文献：

```text
references\\\\NUAA\\\_main\\\_paper.pdf
references\\\\NASA\\\_TM\\\_X\\\_62407.pdf
references\\\\NASA\\\_TM\\\_81244.pdf
```

建议使用的项目子目录：

```text
references\\\\
docs\\\\
tests\\\\
logs\\\\
```

若这些目录尚不存在，只有在当前任务明确需要时才创建。

\---

## 3\. 参考文献的地位

### 3.1 南航文章

```text
references\\\\NUAA\\\_main\\\_paper.pdf
```

该文献是当前项目的主要建模方法参考，用于核查和理解：

* 倾转旋翼机部件划分；
* 旋翼、机翼、机身、平尾、垂尾等分部建模思路；
* 部件气动力和力矩的组合方式；
* 坐标系及坐标变换；
* 六自由度刚体运动方程；
* 配平、数值线性化和稳定性分析流程；
* 文中明确给出的模型假设、公式、图表和参数。

该文献只作为方法和公开信息来源。不得默认其全部参数都适用于当前代码，也不得在没有检查型号、构型、状态、单位和坐标定义的情况下直接复制数据。

### 3.2 NASA 文献

```text
references\\\\NASA\\\_TM\\\_X\\\_62407.pdf
references\\\\NASA\\\_TM\\\_81244.pdf
```

这两份文献是后续核查 XV-15 公开几何、质量、惯量、旋翼、气动、试验或飞行状态数据的候选来源。

在实际引用任何数值前，必须打开原文并核实：

* 文件标题和适用对象；
* 飞行器构型；
* 试验状态；
* 页码；
* 表号、图号或公式号；
* 原始单位；
* 坐标定义；
* 是否为实测值、设计值、估算值或拟合值；
* 是否需要数字化；
* 是否存在不同版本或不同构型之间的冲突。

不得仅凭文件名推测文献内容。

### 3.3 文献引用规则

每次依据文献修改代码或参数时，必须在报告或代码注释中记录：

1. 文献文件名；
2. PDF 页码；
3. 原文页码（若与 PDF 页码不同）；
4. 公式号、表号或图号；
5. 原始符号及定义；
6. 代码中的变量名；
7. 原始单位；
8. 代码单位；
9. 单位换算过程；
10. 坐标或符号转换过程；
11. 该数据的适用构型和工况；
12. 是否仍需人工复核。

无法可靠识别的公式、图片或表格必须标记为“需要人工核对”，不得猜测。

\---

## 4\. 参数来源分类

所有关键参数应尽量标记为以下类别之一：

* `REFERENCE`：直接来自明确文献；
* `DIGITIZED`：从公开图表数字化得到；
* `DERIVED`：由已知参数和明确公式推导得到；
* `ASSUMED`：为初步计算采用的工程假设；
* `PLACEHOLDER`：临时占位，不能用于正式结论；
* `UNKNOWN`：当前没有可靠来源。

不得把 `ASSUMED`、`PLACEHOLDER` 或 `UNKNOWN` 描述为文献实测数据。

不得为了让配平收敛、特征值稳定或图形“看起来合理”而暗中修改参数。

如果参数缺失，应优先：

1. 报告缺失；
2. 判断是否可由已有参数推导；
3. 给出推导公式和前提；
4. 若必须假设，明确标记为 `ASSUMED`；
5. 说明假设对结果的敏感性和风险。

\---

## 5\. 总体工作流程

每次处理任务时，必须遵循以下顺序：

1. 读取本文件 `AGENTS.md`；
2. 明确用户本次任务的范围；
3. 扫描相关文件及其调用关系；
4. 先定位问题和根因；
5. 提出最小修改计划；
6. 在用户要求先计划时，等待用户确认；
7. 只修改完成任务所必需的文件；
8. 运行相应测试；
9. 检查 Git diff；
10. 报告修改、测试和遗留问题。

禁止在未理解调用链的情况下只修复报错所在一行。

禁止为了“让代码跑起来”而：

* 删除物理项；
* 固定函数输出；
* 硬编码配平结果；
* 吞掉报错；
* 用 `try/catch` 隐藏失败；
* 把 NaN、Inf 或复数强制替换成零；
* 无依据调整质量、惯量、几何和气动参数；
* 删除未收敛警告；
* 降低收敛标准但不说明；
* 跳过原本应执行的模型模块。

\---

## 5.1 ChatGPT、GitHub 与 Codex 协作补充

`AGENTS.md` 保存长期规则；`CODEX_TASK.md` 保存当前分支、任务范围、测试和停止条件；GitHub 分支与提交 SHA 作为版本基线；Draft PR 保存 diff、审查意见和 MATLAB 验证状态。

Codex 开始任务前必须同时读取 `AGENTS.md` 与 `CODEX_TASK.md`。工作区存在未提交修改时停止并报告。完成后运行相关测试、检查完整 diff、推送到任务分支并报告提交 SHA。未经用户明确授权不得合并 PR。

当前优先保证：

* 概念参数符合基本物理常理和合理数量级；
* 坐标、符号、单位、控制映射、力和力矩合成正确；
* 程序无明确逻辑、尺寸和数值稳健性错误；
* 配平和线性化结果可回代、可检查；
* 为以后替换 XV-15 或其他型号数据保留接口。

精确 XV-15 参数不构成当前代码修改前提。不得仅因概念参数与 XV-15 不同而替换参数。

在速度精扫、正反扫、大量多初值、逐点 Jacobian、Monte Carlo 或高密度参数扫描前，先估算工况数量、模型调用次数、内部迭代次数和运行时间。默认采用：

```text
单一代表工况快速检查 → 局部针对性检查 → 必要时扩大范围
```

预计运行超过数分钟时先报告并等待确认。

`ASSUMED` 概念参数可以继续使用，前提是数量级合理、语义明确、代码自洽；不得将概念参数描述为型号实测值；不得为了通过测试调整质量、惯量、几何或气动参数；不得隐藏异常、未收敛、NaN、Inf 或复数；测试通过只证明覆盖工况下的内部一致性，不代表型号验证。

\---

## 6\. 修改范围控制

每次任务只处理用户明确指定的问题。

如果发现任务范围外的问题：

1. 记录问题；
2. 标明严重程度；
3. 说明可能影响；
4. 不顺手进行大范围重构；
5. 等待后续任务。

在“只审查”“只读分析”“不要修改代码”等任务中：

* 不得修改任何现有 `.m` 文件；
* 不得修改参数文件；
* 不得自动格式化整个项目；
* 只允许创建用户明确指定的报告文件；
* 完成后必须运行 `git status` 并说明实际改动。

\---

## 7\. MATLAB 编码规则

### 7.1 函数接口

* 不得随意改变现有函数输入输出。
* 修改函数接口前，必须检查所有调用位置。
* 新增输出或参数时，必须同步更新调用者和测试。
* 对缺失参数应明确报错，不得静默填入无来源数值。

### 7.2 向量和矩阵

重点检查：

* 行向量与列向量；
* 3×1 和 1×3 混用；
* 状态向量顺序；
* 控制向量顺序；
* 索引边界；
* MATLAB 隐式扩展；
* 矩阵乘法 `\\\*` 与逐元素运算 `.\\\*`；
* 左除 `\\\\`、右除 `/` 和逐元素除法 `./`；
* 转置 `'` 与非共轭转置 `.'`；
* 矩阵奇异和条件数。

物理向量原则上应采用明确的列向量表示，并在函数入口处核查尺寸。

### 7.3 数值稳健性

必须检查：

* 除零；
* 极小分母；
* `sqrt` 负数；
* `asin`、`acos` 输入越界；
* `atan` 与 `atan2` 的误用；
* 零空速；
* 零转速；
* 零动压；
* 接近 90° 欧拉角奇异；
* 迭代不收敛；
* 复数结果；
* NaN；
* Inf；
* 插值越界；
* 数值微分步长过小或过大。

不得用无说明的 `eps`、绝对值、截断或饱和掩盖物理错误。

### 7.4 注释和可读性

* 新增或修改的关键物理计算应有清晰中文注释或准确英文注释。
* 注释应说明物理意义、坐标系、单位和符号。
* 不要写与代码不一致的注释。
* 不要大范围改变原有格式，除非任务明确要求。

\---

## 8\. 坐标系和符号规则

不得自行假设坐标系。必须从代码、文献、旋转矩阵和运动方程中共同确认。

需要明确并记录：

* 地面坐标系或惯性坐标系；
* 机体坐标系；
* 风轴系；
* 旋翼轴系；
* 左、右旋翼局部坐标系；
* 机翼、尾翼局部坐标系；
* 欧拉角旋转顺序；
* 线速度和角速度定义；
* 力和力矩正方向；
* 短舱倾转角定义；
* 桨距角定义；
* 迎角和侧滑角定义；
* 左右旋翼旋转方向。

若现有代码定义不完整或互相矛盾，必须报告，不得擅自统一。

修改任何坐标变换时，必须给出：

1. 源坐标系；
2. 目标坐标系；
3. 旋转顺序；
4. 旋转矩阵；
5. 主动旋转或被动旋转；
6. 修改前后的符号差异；
7. 一个可验证的极限工况。

\---

## 9\. 单位规则

默认目标单位制为 SI，但必须以代码实际定义为准。

重点检查：

```text
长度        m
速度        m/s
加速度      m/s^2
质量        kg
力          N
力矩        N·m
惯量        kg·m^2
角度        rad
角速度      rad/s
转速        rad/s 或 rpm（必须明确）
密度        kg/m^3
压力        Pa
面积        m^2
功率        W
```

角度必须明确区分 degree 和 radian。

* `sin`、`cos`、`tan` 接受 radian；
* `sind`、`cosd`、`tand` 接受 degree。

不得无说明混用。

rpm 转 rad/s 时应明确使用：

```matlab
omega = rpm \\\* 2\\\*pi / 60;
```

所有文献参数进入代码前必须核查英制与 SI 制换算。

\---

## 10\. 质量、重心和惯量

必须检查：

* 总质量是否与各部件质量一致；
* 重心参考点；
* 部件位置是相对哪个原点定义；
* 惯量是关于部件重心还是整机重心；
* 惯量轴与机体轴是否一致；
* 平行轴定理是否正确；
* 惯量矩阵是否对称；
* 惯量矩阵是否正定；
* 交叉惯量符号；
* 质量和惯量是否随短舱倾转变化；
* 是否存在重复计入部件质量。

不得把缺失的交叉惯量自动设为零后称为真实数据。可以作为工程假设，但必须标记。

\---

## 11\. 力和力矩合成

总力和总力矩必须按统一坐标系合成。

每个部件至少应明确：

* 力的作用点；
* 力所在坐标系；
* 力矩所在坐标系；
* 部件自身气动力矩；
* 力臂产生的力矩；
* 是否已包含参考点转换。

力臂力矩必须核查：

```matlab
M\\\_arm = cross(r, F);
```

其中 `r` 是从总力矩参考点指向力作用点的位置向量。

重点避免：

* `cross(F, r)` 顺序错误；
* 力和位置不在同一坐标系；
* 部件自身力矩与力臂力矩重复；
* 重力重复计算；
* 左右对称部件重复或漏算；
* 推力方向随短舱倾角变化，但作用点或反扭矩方向未同步处理。

\---

## 12\. 旋翼模型

旋翼模型审查应至少覆盖：

* 旋翼半径；
* 桨毂半径；
* 桨叶数；
* 弦长分布；
* 扭转分布；
* 翼型气动数据；
* 桨距定义；
* 转速；
* 旋转方向；
* 叶素径向离散；
* 局部相对速度；
* 入流角；
* 攻角；
* 升力和阻力；
* 推力和扭矩积分；
* 诱导速度；
* 迭代收敛；
* 左右旋翼符号；
* 反扭矩方向；
* 旋翼轴系到机体系的转换；
* 短舱倾角影响；
* 低速和零速极限；
* 是否考虑旋翼间、旋翼—机翼间干扰。

必须区分代码中：

* 标准 BEMT 部分；
* 经验修正；
* 简化假设；
* 临时占位模型；
* 尚未实现的物理效应。

不得将简化模型描述为高保真自由尾迹或 CFD 模型。

\---

## 13\. 机翼、机身和平尾垂尾模型

每个气动部件都应检查：

* 局部来流速度；
* 局部迎角或侧滑角；
* 动压；
* 参考面积；
* 参考弦长或展长；
* 气动系数来源；
* 力方向；
* 力矩方向；
* 部件作用点；
* 坐标转换；
* 低速处理；
* 旋翼尾流影响；
* 遮挡或干扰模型；
* 是否超出系数表有效范围。

若气动系数为临时线性模型，必须明确其适用范围，不得外推后不加说明。

\---

## 14\. 六自由度刚体运动方程

必须核查：

* 状态向量定义及顺序；
* 位置和速度所在坐标系；
* 欧拉角运动学；
* 平动动力学；
* 转动动力学；
* 科氏项和陀螺项；
* 重力投影；
* 惯量耦合；
* 外力和外力矩接口；
* 状态导数顺序；
* 单位一致性。

对于机体系速度和角速度，转动方程应与完整惯量矩阵一致。不得在存在交叉惯量时继续使用只适用于对角惯量矩阵的简化公式，除非明确说明。

\---

## 15\. 配平

配平任务必须明确：

* 飞行状态；
* 固定量；
* 未知量；
* 控制量；
* 目标导数；
* 约束；
* 残差向量；
* 方程数；
* 未知数数量；
* 初值；
* 上下界；
* 残差尺度；
* 收敛判据；
* 求解器退出状态。

不得只以求解器返回“成功”作为配平有效的依据。

配平后必须报告：

* 最终未知量；
* 各残差分量；
* 残差范数；
* 力平衡；
* 力矩平衡；
* 是否触及上下界；
* 是否存在不合理控制量；
* 是否出现 NaN、Inf 或复数；
* 是否对初值敏感。

对称配平必须明确施加对称条件，不能只依赖左右参数“看起来相同”。

\---

## 16\. 数值线性化

数值线性化必须检查：

* 线性化点是否真实配平；
* 状态和控制量顺序；
* 扰动步长；
* 角度扰动单位；
* 前向差分、后向差分或中心差分；
* 不同量级变量的步长缩放；
* 函数在扰动点是否连续；
* 是否跨越限幅、分段或插值边界；
* A、B 矩阵是否包含 NaN、Inf 或复数；
* 结果对扰动步长是否敏感。

优先使用中心差分，并进行至少一个步长敏感性检查。

不得对未配平状态直接给出“稳定性结论”而不说明。

\---

## 17\. 稳定性分析

特征值分析必须结合：

* 线性化状态定义；
* 特征向量；
* 模态频率；
* 阻尼；
* 状态参与度；
* 飞行状态；
* 坐标和单位。

不得只看到实部为负就宣称模型正确。

必须区分：

* 数值稳定；
* 开环动态稳定；
* 模态物理意义；
* 模型参数导致的结果；
* 配平误差导致的伪特征值；
* 线性化步长导致的数值误差。

\---

## 18\. 必须建立的基本测试

在条件允许时，应逐步建立以下测试：

### 18.1 语法和接口测试

* 所有函数可被 MATLAB 解析；
* 输入输出数量正确；
* 无未定义变量；
* 无明显尺寸错误。

### 18.2 有限值测试

检查所有核心函数输出：

```matlab
all(isfinite(value(:)))
```

不得出现 NaN 或 Inf。

### 18.3 左右对称性测试

在完全对称的几何、状态和控制输入下，检查：

* 左右旋翼推力大小；
* 滚转力矩；
* 偏航反扭矩；
* 侧向力；
* 对称部件贡献。

应根据旋向和坐标定义写出预期结果，不能机械要求所有量都相等。

### 18.4 零输入和极限工况测试

至少考虑：

* 零空速；
* 小空速；
* 旋翼停转（若模型允许）；
* 短舱典型角度；
* 零侧滑；
* 零迎角；
* 单部件启用；
* 重力单独启用。

### 18.5 力矩臂测试

人为改变一个部件作用点，验证：

```matlab
delta\\\_M = cross(delta\\\_r, F);
```

方向和大小应与理论一致。

### 18.6 配平回代测试

将配平结果重新代入完整非线性模型，检查状态导数和残差。

### 18.7 线性化一致性测试

用小扰动比较：

```text
非线性模型增量
A·delta\\\_x + B·delta\\\_u
```

二者在足够小扰动下应接近。

\---

## 19\. MATLAB 运行方式

优先在项目根目录运行。

Windows 命令示例：

```bat
cd /d E:\\\\tiltrotor
matlab -batch "run('run\\\_demo.m')"
```

也可使用完整 MATLAB 路径，但必须以本机实际安装路径为准。

若运行单元测试，可使用实际存在的测试入口。不得声称执行了不存在的测试。

若 Codex 当前环境无法调用 MATLAB，必须明确写出：

```text
未进行 MATLAB 实际运行验证。
```

不得根据静态阅读推测“测试已通过”。

\---

## 20\. Git 和变更管理

在修改前后应运行：

```bat
git status
git diff --stat
git diff
```

修改原则：

* 一次只处理一组相关问题；
* 不混入无关格式化；
* 不删除用户文件；
* 不修改参考文献 PDF；
* 不提交大型自动生成临时文件；
* 不覆盖已有报告，除非任务要求；
* 未经用户明确要求，不执行破坏性 Git 命令。

禁止自行执行：

```bat
git reset --hard
git clean -fd
git checkout -- .
```

除非用户明确授权并已确认不会丢失工作。

完成后必须列出：

* 新增文件；
* 修改文件；
* 删除文件；
* 未跟踪文件；
* 是否修改参数；
* 是否修改函数接口。

\---

## 21\. 报告规范

每次任务完成后，回复中至少包括：

1. 读取了哪些文件；
2. 修改了哪些文件；
3. 每项修改的根因；
4. 采用的数学或物理依据；
5. 执行了哪些命令；
6. 是否实际运行 MATLAB；
7. 测试结果；
8. 是否存在 warning；
9. 是否存在 NaN、Inf 或复数；
10. 是否修改参数；
11. 是否存在未验证假设；
12. 尚未解决的问题；
13. 建议的下一步。

审查报告中的问题应分类为：

* MATLAB 语法错误；
* 函数接口错误；
* 变量维度错误；
* 数值计算错误；
* 坐标系或符号错误；
* 单位错误；
* 物理模型错误；
* 模型缺失；
* 参数缺失；
* 文献依据不足；
* 暂时允许的工程简化。

严重程度建议使用：

* `CRITICAL`：阻止运行、造成根本性物理错误或结果不可用；
* `HIGH`：显著影响主要结果；
* `MEDIUM`：局部错误或高风险假设；
* `LOW`：可维护性、注释或轻微稳健性问题；
* `INFO`：说明性记录。

\---

## 22\. 文献—代码对应文件

如需建立文献与代码映射，优先创建或更新：

```text
docs\\\\PAPER\\\_CODE\\\_MAPPING.md
```

建议至少包含：

|文献内容|PDF页码|公式/图/表|代码文件|函数/变量|一致性|备注|
|-|-:|-|-|-|-|-|

一致性分类：

* `EXACT`：数学形式和定义明确一致；
* `SIMILAR`：建模思想相似但实现不同；
* `DIFFERENT`：代码采用不同方法；
* `MISSING\\\_IN\\\_CODE`：文献有、代码缺失；
* `CODE\\\_ONLY`：代码存在、文献未提供；
* `UNVERIFIED`：当前无法可靠判断。

不得仅根据文件名、注释或变量名判断 `EXACT`。

\---

## 23\. 参数来源文件

如需整理参数，优先创建或更新：

```text
docs\\\\PARAMETER\\\_SOURCES.md
```

建议格式：

|参数|代码变量|当前值|单位|来源分类|文献|页码/图表|换算|可信度|备注|
|-|-|-:|-|-|-|-|-|-|-|

任何参数替换都应能从该表追溯。

\---

## 24\. 当前阶段推荐顺序

除非用户另有安排，推荐按以下顺序推进：

1. 扫描项目和函数调用关系；
2. 修复 MATLAB 语法及接口错误；
3. 固化状态量、控制量、坐标系和单位；
4. 检查质量、重心、惯量和部件位置；
5. 检查左右旋翼模型；
6. 检查机翼、机身、平尾和垂尾模型；
7. 检查总力和总力矩合成；
8. 检查六自由度方程；
9. 检查配平；
10. 检查数值线性化；
11. 检查稳定性分析；
12. 建立自动测试；
13. 建立文献—代码映射；
14. 整理参数来源；
15. 再逐步填入和验证 XV-15 公开数据。

\---

## 25\. 禁止事项汇总

Codex 不得：

* 虚构文献、页码、公式号、表号或试验数据；
* 把估计值冒充 XV-15 实测值；
* 把当前模型描述为已经验证的 XV-15 模型；
* 仅为了程序运行而删除物理项；
* 通过随意调参获得稳定结果；
* 隐藏未收敛、NaN、Inf 或复数；
* 未经检查就改变坐标系；
* 未经检查就改变左右旋翼旋向和符号；
* 未经用户授权大范围重构；
* 修改任务范围之外的文件；
* 修改参考文献 PDF；
* 删除用户已有数据或报告；
* 声称运行了 MATLAB，而实际上只做了静态检查；
* 在无法确认时给出确定性结论。

\---

## 25.1 当前控制架构阶段约束

本阶段已确认旋翼控制架构为：

```text
collective / diffCollective / cyclicLong / diffCyclic
```

其中：

* `diffCyclic` 是代码接口中的历史名称；
* 文档中应统一称为 `differentialLongitudinalCyclic`；
* `differentialLongitudinalCyclic` 表示差动纵向周期变距；
* 当前架构物理自洽，不增加 `cyclicLat`；
* 在稳态一阶谐波挥舞模型中，外部 `cyclicLong/diffCyclic`
  先按左右旋翼分配为每侧 `cyclicSide`，旋翼内部再映射为
  `theta1sSide = -rotDir*cyclicSide`；
* 正 `cyclicLong` 定义为使左右盘面法向量共同向 `+eD` 倾斜；
* 正 `diffCyclic` 在当前符号下产生左右相反纵向盘面倾斜和负偏航力矩增量；
* 旧经验盘面倾角模型中的 `diffCyclic -> Fy=0` 结构性零不再适用于正式计算路径。

在控制架构闭环任务中，除非用户明确授权，允许修改范围仅限：

* `AGENTS.md`
* `README.md`
* `docs/CONTROL_CONVENTIONS.md`
* `tests/check_control_architecture.m`
* `tests/run_all_checks.m`

禁止修改：

* `model/`
* `analysis/`
* `params_nominal.m`
* `tests/check_article_trends.m`

控制架构测试必须覆盖：

* 对称总距；
* 差动总距；
* 对称纵向周期变距；
* 差动纵向周期变距；
* `+diff/-diff` 镜像关系；
* `h = 1e-3, 1e-4, 1e-5` 差分稳定性。

若控制架构测试失败，只能修正测试定义、容差或文档。不得为了通过该测试修改模型、参数、旋翼符号、坐标变换或既有物理项。无法通过的模型行为应记录为未解决项。

\---

## 25.2 当前机翼 near-normal 连续化状态

`model/wing_model.m` 中 near-normal 与 lift-line 仍是两套概念气动模型。
正式载荷不再使用 `nearNormal` 布尔量硬切换，而是在同一局部来流状态下
分别计算 `FNear` 与 `FLiftLine`，再用 quintic smootherstep 权重连续混合。

`P.wing.normalFlowRatio = 0.35` 表示过渡中心；
`P.wing.normalFlowBlendHalfWidth = 0.15` 是
`ASSUMED_MODEL_PARAMETER`，来自本项目低速配平连续性敏感性，不是文献或试验数据。

该连续化只用于避免人工载荷跳变，不表示机翼气动模型已经完成试验验证。

## 26\. 完成标准

一个代码任务只有在满足以下条件时才算完成：

* 问题根因已说明；
* 修改范围与任务一致；
* 修改是最小且可追踪的；
* MATLAB 已实际运行，或明确声明无法运行；
* 相关测试已执行；
* 输出无新的 NaN、Inf 或复数；
* 没有暗中调整物理参数；
* Git diff 已检查；
* 未解决问题已列出；
* 文献和参数来源可追溯；
* 不夸大模型精度和验证状态。



